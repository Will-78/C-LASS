# C-LASS
Developing AI-Supported Learning Environments for Static Analysis and Cybersecurity.

You should be able to start up the stack locally using
```
docker-compose up --build
```
from the main directory of the repository. 

Then access the site at
```
http://localhost/
```